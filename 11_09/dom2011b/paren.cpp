/** @file paren.cpp @brief 世界の天秤を解くプログラムで用いる関数
 *
 *  @see http://icpc.iisf.or.jp/past-icpc/domestic2011/contest/all_ja.html
 */
#include "dom2011b/paren.h"
#include <stack>
#include <string>
using namespace std;

bool match_parens(const string& s) {
	// 文字(char)のスタックを用意する。

	// 文字列sの先頭から順に各文字について以下を繰り返す。
	for () {
		if () {  // 開き括弧の場合
			// その括弧をプッシュする。

		} else if () {  // 閉じ括弧の場合
			if () { // スタックが空だったり、スタックのトップが対応する開き括弧ではなければ
				// falseを返す。
			}

			// ポップする。
		}
	}

	// スタックが空ならばtrue、そうでなければfalseを返す。
}
